Add VAE and do generation in it's latent space

Try $N(0, 1)$ instead of $[-1; 1]$ in normalization

Add more metadata to trajectories: trajectory length, sharpness, costs, min distance to obstacle; Add length / cost / cls token to context

Positional encodings - random fourier vs learned fourier vs default sinusoidal

Attention or no attention
